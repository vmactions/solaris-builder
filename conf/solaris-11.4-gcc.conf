
# TODO: Replace me with new linke in https://github.com/vmactions/solaris-download/releases/
VM_ISO_LINK="https://github.com/bradh352/vmactions-solaris-builder/releases/download/2.0.0/solaris-11.4.42.111.0-x86-cbe.iso"
VM_OS_NAME="solaris"
VM_OS_TYPE="solaris11"
VM_SSH_PORT=2223

VM_ROOT_PASSWORD="vmactions.org"

VM_RELEASE=11.4-gcc

VM_SHUTDOWN_CMD="shutdown -y -i5 -g0"

VM_INSTALL_CMD="pkg install --no-backup-be --no-refresh --accept -v"

# Not needed, included in VM_PRE_INSTALL_PKGS
#VM_RSYNC_PKG="rsync"
#VM_SSHFS_PKG=""

# Virtio support added in Solaris 11.4.33, use "ide" for older versions.
VM_DISK="virtio,discard=unmap"

# Note: rsync is a base package, no need to specifically install it.
# We are installing C and C++ compilers only, not things like fortran or objc
# to reduce the disk space requirements.
VM_PRE_INSTALL_PKGS="autoconf automake developer/gcc/gcc-c developer/gcc/gcc-c++ libtool pkg-config gnu-make"

VM_LOGIN_TAG="solaris console login"
VM_OPTS="conf/solaris-11.4.opts.txt"

VM_VBOX="./vbox.sh"

VM_VBOX_LINK="https://raw.githubusercontent.com/vmactions/vbox/v0.9.6/vbox.sh"
